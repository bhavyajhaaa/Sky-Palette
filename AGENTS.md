# Sky Palette

Before making visual or interaction changes, read:

- `docs/design.md`

For the current redesign sequence, see:

- `docs/redesign-plan.md`

General rules:
- Preserve working behavior unless the task explicitly changes it.
- Work in small, scoped iterations.
- Do not implement multiple redesign stages in one task.
- Run the project's existing typecheck/build checks after changes.