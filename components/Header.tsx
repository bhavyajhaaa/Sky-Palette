'use client';import Link from 'next/link';import {usePathname} from 'next/navigation';import {useEffect,useState} from 'react';import {useAddSky} from './AddSkyModal';
export function Header(){const path=usePathname(),{open}=useAddSky(),[dark,setDark]=useState(false);useEffect(()=>setDark(document.documentElement.classList.contains('dark')),[]);const toggle=()=>{const v=!dark;setDark(v);document.documentElement.classList.toggle('dark',v);localStorage.setItem('sky-theme',v?'dark':'light')};return <header className="h-16 px-5 md:px-8 flex items-center justify-between border-b line relative z-20 bg-[var(--bg)]"><Link href="/" className="site-mark text-[13px] font-semibold tracking-[.14em]">SKY COLORS</Link><nav className="flex items-center gap-4 md:gap-7 text-xs"><Link className={path==='/'?'':'muted'} href="/">Palette</Link><Link className={path==='/fluid'?'':'muted'} href="/fluid">Fluid</Link><Link className={(path==='/archive'||path.startsWith('/sky/'))?'':'muted'} href="/archive">Archive</Link><button className="muted text-base leading-none" aria-label="Toggle theme" onClick={toggle}>{dark?'☼':'◐'}</button><button onClick={open} className="hidden sm:block border-b line pb-0.5">＋ Add Sky</button><button onClick={open} aria-label="Add sky" className="sm:hidden text-lg">＋</button></nav></header>}
