'use client';import {sortColors} from '@/lib/images';
export function PaletteGrid({colors,skies}:{colors:string[];skies:number}){const sorted=sortColors(colors);return <main className="relative min-h-[calc(100vh-4rem)]"><div className="grid grid-cols-3 sm:grid-cols-4 lg:grid-cols-6 min-h-[calc(100vh-4rem)] auto-rows-[18vw] sm:auto-rows-[13vw] lg:auto-rows-[9vw]">{sorted.map((color,i)=><button key={i} style={{backgroundColor:color}} className="group relative" aria-label={`Color ${color}`}><span className="absolute inset-x-0 bottom-2 text-[10px] font-medium opacity-0 group-hover:opacity-70 transition-opacity" style={{color:parseInt(color.slice(1),16)>0x888888?'#111':'#fff'}}>{color}</span></button>)}</div><div className="fixed bottom-4 left-5 z-10 bg-[var(--bg)]/75 px-2 py-1 text-[11px] backdrop-blur-sm">{colors.length} colors from {skies} skies</div></main>}
