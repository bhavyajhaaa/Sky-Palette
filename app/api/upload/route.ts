import {NextResponse} from 'next/server';import {serverSupabase} from '@/lib/supabase';import {randomUUID} from 'crypto';
export async function POST(req:Request){const form=await req.formData();if(!process.env.SKY_UPLOAD_PASSWORD||form.get('password')!==process.env.SKY_UPLOAD_PASSWORD)return NextResponse.json({error:'Incorrect upload password.'},{status:401});const image=form.get('image');if(!(image instanceof File)||image.type!=='image/webp'||image.size>250_000)return NextResponse.json({error:'Invalid image.'},{status:400});const supabase=serverSupabase();if(!supabase)return NextResponse.json({error:'Supabase is not configured yet.'},{status:503});let colors:string[];try{colors=JSON.parse(String(form.get('colors')))}catch{return NextResponse.json({error:'Invalid color data.'},{status:400})}if(!Array.isArray(colors)||colors.length>20||colors.some(c=>!/^#[0-9A-F]{6}$/.test(c)))return NextResponse.json({error:'Invalid color data.'},{status:400});const id=randomUUID(),path=`${id}.webp`;const bytes=new Uint8Array(await image.arrayBuffer());const up=await supabase.storage.from('sky-images').upload(path,bytes,{contentType:'image/webp'});if(up.error)return NextResponse.json({error:up.error.message},{status:500});const row=await supabase.from('skies').insert({id,image_path:path,width:Number(form.get('width')),height:Number(form.get('height')),colors}).select().single();if(row.error)return NextResponse.json({error:row.error.message},{status:500});return NextResponse.json({sky:row.data})}
